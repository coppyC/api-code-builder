{{#formData parameters}}
const fd = new FormData()
{{#each fd}}
fd.append('{{name}}', {{name}})
{{/each}}
{{else}}
const fd = new FormData()
for(let name in formData)
  fd.append(name, formData[name])
{{/formData}}
